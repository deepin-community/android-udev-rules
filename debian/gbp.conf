[DEFAULT]
dist = DEP14
debian-branch = debian/latest
upstream-branch = upstream/latest
sign-tags = True
# Tarball is generated with git deborig, according to dgit-maint-debrebase(7)
pristine-tar = False
pristine-tar-commit = False
